# Deprecated runtime model entry

The earlier managed-Worker Runtime domain model has been superseded.

Canonical product domain model is now:

```text
docs/channel-model.md
```

The core MCP models existing terminal Channels only. Worker identity/lifecycle, Agent type, Task mapping and project coordination are outside the product boundary.

This file remains only for old links/history and is not authoritative for new implementation work.
