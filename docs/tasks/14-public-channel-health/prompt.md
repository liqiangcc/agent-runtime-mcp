# Session Bootstrap — MVP-002.5 Public Channel health surface

You are a separate GPT Web Worker for Issue #14 in `liqiangcc/agent-runtime-mcp`.

Task Contract:

```text
docs/tasks/14-public-channel-health/task.md
```

Current expected state: `status:draft`.

Do not claim or implement while draft. When a future Coordinator publishes it to `status:ready`, use `@GitHub` live state, follow repository claim/Attempt/report protocol, use GitHub Actions for required executable Evidence, then stop at `status:review` or `status:blocked`.

Do not implement remote ingress or Review/close the Task yourself.
