# Deprecated architecture entry

The earlier managed-Worker Runtime architecture has been superseded.

Canonical product architecture is now:

```text
docs/channel-architecture.md
```

The product is a generic terminal Channel MCP. Worker/Agent/Task/worktree/tmux-session lifecycle belongs to upper-layer collaboration or deployment logic.

This file is retained only to preserve repository history and old links. Do not use it as architecture authority for new implementation work.
